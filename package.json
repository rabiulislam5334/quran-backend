{
  "name": "quran-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "npm install && npx tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@hono/node-server": "^2.0.1",
    "hono": "^4.12.18",
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "latest"
  },
  "engines": {
    "node": ">=18"
  }
}