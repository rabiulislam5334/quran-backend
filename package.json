{
  "name": "quran-backend",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build ./src/index.ts --outdir ./dist"
  },
  "dependencies": {
    "hono": "^4.12.18"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "latest",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}